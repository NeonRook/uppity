[tools]
actionlint = "1"
bun = "1.3.14"

[tasks.test]
description = "Run unit tests with DATABASE_URL loaded from .env"
run = '''
#!/usr/bin/env bash
set -a
source .env
set +a
bun run test:unit run "$@"
'''
