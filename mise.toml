[tools]
actionlint = "1"
aube = "1.41.0"
node = "26"
# Still installed because .github/actions/setup-bun pins the CI toolchain from
# this file. Removed once CI runs on node and aube.
bun = "1.3.14"

[env]
_.path = ["{{config_root}}/node_modules/.bin"]

# Absent in CI, which sets these in the workflow. mise skips a missing file.
_.file = ".env"
