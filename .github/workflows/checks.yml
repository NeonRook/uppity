name: Checks

on:
  workflow_call:

permissions:
  contents: read

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-26.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-toolchain
      - run: aubr lint:ci

  typecheck:
    name: Type Check
    runs-on: ubuntu-26.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-toolchain
      - run: aubr check

  unit:
    name: Unit Tests
    runs-on: ubuntu-26.04
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/postgres
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-toolchain
      # The "client" vitest project runs in a real browser but pins chromium alone,
      # so installing firefox and webkit is pure runner time.
      - run: aube x playwright install --with-deps chromium
      - run: aubr test:unit run

  e2e:
    name: E2E Tests
    runs-on: ubuntu-26.04
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/postgres
      VITE_BETTER_AUTH_URL: http://localhost:4173
      # better-auth reads this from process.env at runtime; the build only needs a
      # non-empty value to be present. Never the real secret.
      BETTER_AUTH_SECRET: ci-placeholder-not-a-real-secret
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-toolchain
      - run: aube x playwright install --with-deps chromium
      - run: aubr db:migrate
      - run: aubr test:e2e

  build:
    name: Build
    runs-on: ubuntu-26.04
    env:
      VITE_BETTER_AUTH_URL: http://localhost:5173
      BETTER_AUTH_SECRET: ci-placeholder-not-a-real-secret
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-toolchain
      - run: aubr build:all

# Dependency auditing does not live here. A PR-only audit learns nothing during a quiet
# week with no dependency-touching PRs, so it runs on a schedule (plus pull requests) in
# audit.yml, using trivy fs to report SARIF to the Security tab, non-blocking.
